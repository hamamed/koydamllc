# ---- Server ----
PORT=3000
NODE_ENV=development

# ---- Admin auth ----
# Long random string used to sign the admin session cookie.
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=change-me-to-a-long-random-string

# Admin login. Prefer the hash; ADMIN_PASSWORD is a dev-only fallback.
ADMIN_EMAIL=hello@koydam.com
ADMIN_PASSWORD=changeme
# Generate a hash: node server/scripts/hash-password.js "your password"
ADMIN_PASSWORD_HASH=

# Session lifetime in hours
SESSION_HOURS=12

# ---- Contact form email (optional) ----
# Leave SMTP_HOST empty to just store inquiries in the DB without emailing.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_TO=hello@koydam.com
MAIL_FROM="Koydam Website <no-reply@koydam.com>"
